.anchor
.DS_Store
**/*.rs.bk
node_modules
test-ledger
.yarn
/proof_pol
 ./ runbooks
/.surfpool
/runbooks
txtx.yml
.codex

# Rust / Anchor build output 
target/debug/
target/release/
target/sbpf-solana-solana/
target/bpfel-unknown-unknown/
target/deploy/

# Program authority keypairs must never be committed
**/*-keypair.json

# Tracked on purpose (small, not secret — public program interface):
#   target/idl/proof_pol.json   → CI keeper, Anchor.load, Codama

